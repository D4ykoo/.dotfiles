## Requirements
See documentation of the submodules  
For easier use: the GNU `stow` project
```bash
stow <config-name>
```
### i3
* `feh`
* `rofi`
* `polybar`

## Init Submodules 
git submodule update --init
